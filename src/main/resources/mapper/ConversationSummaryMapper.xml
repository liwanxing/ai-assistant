<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.liwx.aiassistant.chat.mapper.ConversationSummaryMapper">

    <select id="selectBySessionId" resultType="com.liwx.aiassistant.chat.entity.ConversationSummary">
        SELECT id, session_id, summary, summarized_up_to, update_time
        FROM rag_conversation_summary
        WHERE session_id = #{sessionId}
    </select>

    <insert id="upsert">
        INSERT INTO rag_conversation_summary (session_id, summary, summarized_up_to)
        VALUES (#{sessionId}, #{summary}, #{summarizedUpTo})
        ON DUPLICATE KEY UPDATE
            summary = #{summary},
            summarized_up_to = #{summarizedUpTo}
    </insert>

    <!-- 删除会话摘要：删会话/定时清理时调用，防止孤儿摘要 -->
    <delete id="deleteBySessionId">
        DELETE FROM rag_conversation_summary WHERE session_id = #{sessionId}
    </delete>

</mapper>
